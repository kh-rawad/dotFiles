alias ls="ls --color" \
	la="ls -A" \
	ll="ls -l" \
	lll="ls -la" \
	#alias l='ls -CF'


alias vis="sudo vi"
alias ping="ping -c3"
alias ctl="sudo systemctl"
alias mkdir='mkdir -p'
alias h='history'
alias j='jobs -l'
alias which='type -a'
alias ..='cd ..'
alias du='du -kh'    # Makes a more readable output.
alias df='df -kTh'
alias suod='sudo'    # personal issue
